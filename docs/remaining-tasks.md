# Remaining Tasks — ozon-product-parser

Актуальный список задач после аудита проекта от 22 сентября 2026.

## Текущий статус

Подтверждено:
- [x] PostgreSQL 16 + SQLAlchemy 2 + psycopg 3.
- [x] Alembic migration для таблицы `products`.
- [x] UPSERT по `sku`.
- [x] Docker Compose.
- [x] CI в GitHub Actions.
- [x] Ruff.
- [x] Pytest.
- [x] Интеграционные тесты PostgreSQL через отдельную `*_test` базу.
- [x] `get_cookies.py` и `parse_ozon.py` разделены.
- [x] Защита от сохранения antibot HTML как карточки товара.
- [ ] Подтвердить зелёный GitHub Actions на текущем `main`: ранние запуски падали на форматировании, последние jobs завершаются без runner и без шагов из-за ограничения Billing & plans (неудавшийся платёж или лимит расходов по сообщению GitHub). Ранее успешный запуск был на старом коммите.

Не подтверждено:
- [ ] Реальный login на `data.ozon.ru`.
- [ ] Реальное получение письма с кодом Ozon через Gmail API (OAuth и `users.getProfile` подтверждены; текущий адрес доставки кода не Gmail).
- [ ] Пригодность сохранённых cookies для запросов карточек.
- [ ] Фактическая структура JSON в живой карточке Ozon.
- [ ] Извлечение всех требуемых полей из реальной карточки.
- [ ] Полный end-to-end запуск Ozon → parser → PostgreSQL.

---

## P0 — Закрыть сейчас без live Ozon

### 1. Hardening браузерной сессии и cookies

- [x] Сохранять User-Agent браузера, в котором выполнялась авторизация.
- [x] Убрать жёстко заданный Linux Chrome User-Agent из `product_session()`.
- [x] Использовать при parsing тот же User-Agent, с которым получена браузерная сессия.
- [x] Сохранять необходимые cookie-атрибуты: `domain`, `path`, `secure`, `expires`.
- [x] Игнорировать явно истёкшие cookies.
- [x] Проверить границы домена `ozon.ru` и поддоменов.
- [x] Добавить unit tests на перенос cookies и browser session metadata.
- [x] Не логировать значения cookies, verification code и номер телефона.

Примечание: старый формат списка cookies читается с User-Agent по умолчанию `requests`; совпадение User-Agent с браузером гарантируется для нового формата. Реальная совместимость с Ozon требует live-проверки.\n\nКритерий готовности:
- cookies/session metadata сериализуются безопасно;
- requests-клиент воспроизводит браузерные заголовки;
- тесты проверяют валидные, истёкшие и чужие cookies.

### 2. Усилить Gmail adapter

- [x] Покрыть `wait_for_code()` unit tests.
- [x] Старое письмо до начала login attempt не должно приниматься.
- [x] Письмо не от Ozon не должно приниматься.
- [x] Письмо без verification code не должно приниматься.
- [x] Поддержать `text/plain`.
- [x] Поддержать `text/html`.
- [x] Поддержать multipart MIME.
- [x] Проверить timeout.
- [x] Расширить распознавание форматов кода, например:
  - `Ваш код: 123456`;
  - `123456 — код для входа`;
  - `Code 123456`.
- [x] Вынести Gmail query / ожидаемого отправителя в конфигурацию.
- [x] Сделать фильтр отправителя строже текущей проверки по слову `ozon`.
- [x] Проверить корректное восстановление OAuth token через refresh token.

Примечание: обновление OAuth refresh token проверено без реального Google; фактический отправитель и OAuth login требуют live-проверки в P1.\n\nКритерий готовности:
- Gmail adapter можно тестировать без реального Gmail;
- verification code извлекается из нескольких типовых форматов;
- старые и посторонние письма гарантированно игнорируются.

### 3. Усилить extractor до получения реальной карточки

- [x] Поддержать рекурсивное декодирование JSON-строк внутри embedded JSON.
- [x] Не декодировать произвольный текст как JSON без проверки.
- [x] Добавить тесты на вложенные dict/list/JSON-string структуры.
- [x] Добавить тест на несколько товаров в одном состоянии страницы.
- [x] Добавить тест на несколько цен и явный приоритет источника цены.
- [x] Добавить тест на `rating` / `reviews_total`.
- [x] Добавить тест на `cover_image`.
- [x] Добавить тесты для `photos_seller` и `videos_seller`.
- [x] Добавить тесты для `color`, `material`, `art_set`.
- [x] Зафиксировать приоритет для `art_set`, если одновременно есть:
  - артикул производителя;
  - комплектация;
  - состав набора.
- [x] Усилить `has_rich_content` для отдельного rich-content widget, если он появляется как отдельная структура.
- [x] Не считать картинки из отзывов фотографиями продавца.
- [x] Не принимать объект с несовпадающим SKU.

Примечание: имена `photosSeller`/`videosSeller` и расположение rich-content widget подтверждены только синтетическими тестами; фактические поля Ozon остаются в P1, задача 10. Приоритет `art_set`: артикул производителя → art set → комплектация → состав набора. Приоритет цены: первый корректный `offers.price` → `product.price`.\n\nКритерий готовности:
- extractor работает с JSON-LD и nested serialized JSON;
- каждое обязательное поле имеет отдельный тест;
- логика выбора объекта строго привязана к SKU.

### 4. Добавить offline/debug режим

- [x] Добавить возможность разобрать сохранённый HTML без запроса в Ozon.
- [x] Пример интерфейса:
  `python scripts/parse_ozon.py 2359066702 --html-file captured.html`
- [x] Offline режим должен использовать тот же `extract_product()`, что и live режим.
- [x] Не сохранять live HTML автоматически по умолчанию.
- [x] Добавить явный debug-флаг для сохранения обезличенной страницы при необходимости.
- [x] Документировать риск наличия чувствительных данных в сохранённом HTML.
- [x] Добавить tests для offline parsing.

Примечание: debug-снимок удаляет типичные секреты, но не гарантирует полной анонимизации; перед публикацией требуется ручная проверка. Offline режим не требует cookies/Ozon, но записывает данные в PostgreSQL.\n\nКритерий готовности:
- после однократного получения реальной карточки extractor можно дорабатывать без повторных обращений к Ozon.

### 5. Покрыть CLI тестами

#### parse_ozon.py
- [x] Невалидный SKU.
- [x] Отсутствующий `cookies.json`.
- [x] Повреждённый `cookies.json`.
- [x] Отсутствующий `DATABASE_URL`.
- [x] Успешная обработка одного SKU.
- [x] Успешная обработка нескольких SKU.
- [x] Частичная ошибка одного SKU при успешных остальных.
- [x] Exit code `0` при полном успехе.
- [x] Exit code `1` при ошибке parsing одного или нескольких SKU.
- [x] Exit code `2` при ошибке начальной конфигурации.
- [x] CSV export.
- [x] Создание родительского каталога CSV.
- [x] CSV содержит только успешно обработанные товары.

#### get_cookies.py
- [x] Невалидный `OZON_PHONE`.
- [x] Отсутствующий `OZON_PHONE`.
- [x] Невалидный browser name.
- [x] Невалидный browser channel.
- [x] Channel запрещён для Firefox/WebKit.
- [x] Antibot page приводит к отказу без сохранения cookies.
- [x] Cookies не сохраняются при незавершённой авторизации.
- [ ] Подтвердить login по защищённому ресурсу Ozon перед сохранением cookies (нужен live доступ).
- [x] Проверить permissions файла с cookies там, где ОС поддерживает chmod.

Примечание: offline-тесты проверяют, что cookies не сохраняются при ошибке и сохраняются после успешного возврата `authenticate()`. Реальный защищённый признак успешного входа пока неизвестен; проверка остаётся открытой вместе с P1/8.\n\nКритерий готовности:
- основные CLI сценарии проверяются автоматически без live Ozon.

### 6. Обновить документацию\n\nЛокально: Ruff и форматирование пройдены, 79 passed и 10 skipped без PostgreSQL. README содержит полный порядок настройки своей почты, БД и запуска; CI на текущем `main` остаётся красным до устранения ограничения Billing & plans у владельца аккаунта.

- [x] Обновить `docs/task-audit.md`: CI уже подтверждён.
- [x] Обновить README со статусом PostgreSQL/Alembic/tests.
- [x] Явно разделить:
  - подтверждено автоматически;
  - требует live Ozon;
  - optional/bonus.
- [x] Описать session metadata / User-Agent.
- [x] Описать offline HTML mode.
- [x] Описать безопасную работу с OAuth token / cookies.
- [x] Указать точную последовательность локального запуска.
- [x] Указать точную последовательность финальной end-to-end проверки.

---

## P1 — Требует live Ozon / Gmail

### 7. Проверить Gmail OAuth на реальном аккаунте

Реальный OAuth Desktop Client настроен пользователем: `scripts/check_gmail.py` успешно выполнил `users.getProfile` через Gmail API (`INFO Gmail API access confirmed; OAuth token is usable`). `--wait-for-code` и фактическое письмо Ozon ещё не проверялись. `credentials.json` и `token.json` хранятся только в окружении пользователя, не в репозитории.

- [x] Создать/подключить Google Cloud OAuth Desktop Client.
- [x] Включить Gmail API и подтвердить доступ запросом `users.getProfile`.
- [x] Получить `credentials.json` (на машине пользователя).
- [x] Выполнить первый OAuth login.
- [x] Проверить наличие `token.json` на машине пользователя (подтверждено командой `test -f token.json`).
- [ ] Проверить получение именно нового письма Ozon после возврата Gmail как адреса кода либо согласования изменения ТЗ.
- [ ] Подтвердить реальный формат From / Subject / MIME / verification code.
- [ ] Скорректировать Gmail filters по реальным данным после получения письма на Gmail.

Критерий готовности:
- скрипт автоматически получает именно свежий Ozon verification code через Gmail API, как требует ТЗ. Если Ozon продолжит отправлять код на другой сервис, отдельно согласовать изменение способа получения кода с заказчиком; не выдавать его за выполнение Gmail-пункта.

### 8. Проверить login на data.ozon.ru

- [ ] Запустить `get_cookies.py` в разрешённой среде.
- [ ] Проверить реальные selectors/roles/placeholder.
- [ ] Проверить переход из `data.ozon.ru` в auth/SSO и обратно.
- [ ] Проверить auto-submit verification code.
- [ ] Проверить сценарий с кнопкой подтверждения.
- [ ] Проверить, что login считается успешным не только по URL.
- [ ] Найти защищённый признак успешной авторизации.
- [ ] Сохранить только Ozon cookies.
- [ ] Проверить срок жизни cookies.
- [ ] Не добавлять обход CAPTCHA/antibot.

Критерий готовности:
- после успешного запуска создаётся пригодная для parsing browser session.

### 9. Проверить перенос cookies browser → requests

Промежуточно подтверждено локально: экспорт cookies из обычного Chrome успешно преобразован через `scripts/import_browser_cookies.py`; создан `cookies.json`, а `product_session()` принял 10 пригодных Ozon cookies. Это подтверждает импорт готовой браузерной сессии, но ещё не подтверждает доступ к карточкам через requests и не подтверждает автоматический вход через Gmail.

- [x] Загрузить реальные cookies из обычного Chrome в формат parser session.
- [ ] Запросить реальную карточку через requests.Session.
- [ ] Проверить отсутствие redirect на login.
- [ ] Проверить отсутствие antibot response.
- [ ] Проверить совместимость User-Agent и cookies.
- [ ] При необходимости добавить browser-derived headers.
- [ ] Зафиксировать минимальный набор headers, действительно необходимый для работы.

Критерий готовности:
- карточка товара получается через requests с сохранённой авторизованной сессией.

### 10. Получить и изучить настоящий HTML/JSON карточки

- [ ] Получить минимум одну доступную карточку.
- [ ] Сохранить локальную обезличенную fixture.
- [ ] Не коммитить чувствительные session/account данные.
- [ ] Найти фактический product state.
- [ ] Зафиксировать структуру Ozon widgets/state.
- [ ] Определить реальные пути до каждого поля.
- [ ] Обновить extractor adapters.

Обязательные поля для подтверждения:
- [ ] `sku`
- [ ] `title`
- [ ] `price`
- [ ] `rating`
- [ ] `reviews_total`
- [ ] `cover_image`
- [ ] `photos_seller`
- [ ] `videos_seller`
- [ ] `color`
- [ ] `material`
- [ ] `art_set`
- [ ] `has_rich_content`

Критерий готовности:
- все требуемые поля извлекаются из реальной структуры, а не только из синтетической fixture.

### 11. Финальный end-to-end тест

- [ ] Поднять PostgreSQL.
- [ ] Применить Alembic.
- [ ] Получить cookies.
- [ ] Запустить parser по реальному SKU.
- [ ] Проверить INSERT.
- [ ] Повторно запустить тот же SKU.
- [ ] Проверить UPDATE.
- [ ] Убедиться, что дубликат строки не появился.
- [ ] Проверить значения всех доступных полей.
- [ ] Проверить exit code.
- [ ] Проверить логирование без секретов.
- [ ] При необходимости проверить несколько SKU за один запуск.

Критерий готовности:
- полный сценарий login → Gmail → cookies → Ozon product → extractor → PostgreSQL проходит успешно.

---

## P2 — Финальная подготовка к сдаче

- [ ] Удалить временные debug artifacts.
- [ ] Проверить `.gitignore`.
- [ ] Убедиться, что в Git отсутствуют:
  - `credentials.json`;
  - `token.json`;
  - `cookies.json`;
  - `.env`;
  - сохранённый чувствительный HTML.
- [ ] Добиться успешного CI на текущем `main`; владелец аккаунта должен устранить указанное GitHub ограничение платежа/лимита в Billing & plans, затем повторить workflow и проверить Ruff, миграцию и все тесты.
- [ ] Проверить README с чистой машины/окружения.
- [ ] Проверить Docker Compose workflow.
- [ ] Проверить команды из README буквально копированием.
- [ ] Зафиксировать known limitations.
- [ ] Обновить статус проекта с «в разработке» на фактический после live проверки.

---

## P3 — Bonus only

Эти задачи выполнять только после полностью рабочего основного сценария.

### Airflow
- [ ] DAG для периодического запуска parsing.
- [ ] Передача списка SKU.
- [ ] Retry/backoff.
- [ ] Логи.
- [ ] Не хранить cookies/credentials внутри DAG source.

### DataLens
- [ ] Подключение PostgreSQL.
- [ ] Dataset по `products`.
- [ ] Минимальный dashboard:
  - SKU;
  - title;
  - price;
  - rating;
  - reviews;
  - updated_at.

---

## Рекомендуемый порядок выполнения

1. [x] Session/cookies hardening.
2. [x] Gmail adapter tests/hardening.
3. [x] Nested JSON extractor.
4. [x] Offline HTML mode.
5. [x] CLI tests (кроме live подтверждения входа).
6. [x] README + task-audit update.
7. [ ] Real Gmail OAuth подтверждён; осталось проверить новое письмо Ozon и его формат.
8. [ ] Real Ozon login.
9. [ ] Browser → requests session validation.
10. [ ] Real HTML fixture + final extractor mapping.
11. [ ] End-to-end PostgreSQL validation.
12. [ ] Final cleanup and submission.
13. [ ] Airflow/DataLens only if time remains.

## Definition of Done

Проект можно считать готовым к сдаче, когда одновременно выполнено:

- [ ] GitHub Actions зелёный.
- [ ] Все unit/integration tests зелёные.
- [ ] Реальный Gmail verification code получен автоматически.
- [ ] Реальная авторизация Ozon успешно завершена.
- [ ] Cookies пригодны для загрузки карточек.
- [ ] Все поля задания извлекаются из реальной карточки.
- [ ] Один SKU успешно записывается в PostgreSQL.
- [ ] Повторный запуск обновляет строку без создания дубликата.
- [ ] README полностью воспроизводит запуск проекта.
- [ ] Репозиторий не содержит credentials, OAuth tokens, cookies и других секретов.
