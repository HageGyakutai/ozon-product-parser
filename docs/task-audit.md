# Аудит тестового задания — 22 сентября 2026

## Подтверждено автоматически

- Ранее GitHub Actions проходил с PostgreSQL 16, Alembic, Ruff и pytest на отдельной тестовой БД. Последние запуски GitHub Actions имеют статус failure: часть ранних остановилась на `ruff format --check .`, последние jobs завершаются до выделения runner (нет выполненных шагов). В аннотации GitHub указана причина: недавние платежи аккаунта не прошли либо нужно увеличить лимит расходов в Billing & plans. После устранения ограничения требуется новый зелёный запуск; форматирование локально проходит.
- Локально: Ruff и форматирование пройдены, 79 тестов пройдены, 10 PostgreSQL-тестов пропущены без `TEST_DATABASE_URL`. Это не результат полного CI.
- Compose запускает БД, ждёт healthy и выполняет миграцию до контейнера парсера. Пользовательский запуск дошёл до проверки `cookies.json` и остановился из-за его отсутствия.
- Offline тесты проверяют перенос User-Agent, домен/path/secure/expires cookies, отказ от истёкших cookies, MIME и время Gmail-письма, код из нескольких форматов, выбор SKU, вложенный JSON, 12 полей на синтетических данных, коды завершения CLI и экспорт CSV.

## Требует реального Ozon и Gmail

| Компонент | Что ещё доказать |
|---|---|
| Вход через `data.ozon.ru` | Фактические элементы формы и подтверждение входа по защищённому ресурсу, а не одному URL. Сейчас Playwright получает antibot до формы. |
| Gmail API | Реальный OAuth и `users.getProfile` успешно проверены пользователем; Ozon теперь отправляет письмо на почту другого провайдера. Требуемый ТЗ путь через Gmail API ждёт возможности вернуть Gmail как адрес кода либо согласования изменений ТЗ. Отправитель, формат и свежий код не подтверждены. |
| Browser → requests | Импортированная Chrome-сессия корректно читается, но `requests.Session` и Playwright-launched Chromium получают HTTP 403 до extractor; внешний Chrome/CDP недоступен из-за системной policy `RemoteDebuggingAllowed = 0`. |
| Карточка товара | Реальный HTML SKU `2359066702` успешно разобран. В стандартных parseable JSON-блоках найден только один JSON-LD `Product` с базовыми ключами `@context,@type,aggregateRating,brand,description,image,name,offers,sku`; дополнительных полей media/characteristics/richContent там нет. Добавлен расширенный безопасный инспектор всех script/state-блоков и rendered DOM. |
| PostgreSQL end-to-end | Реальный HTML SKU `2359066702` успешно прошёл extractor и запись в PostgreSQL (`EXIT_CODE=0`). Повторный запуск сохранил тот же `id=1` и `created_at`, изменил `updated_at`: UPSERT подтверждён. В SELECT заполнены `sku`, `title`, `price`, `rating`, `reviews_total`, `cover_image`; шесть остальных полей ещё требуют проверки структуры реального HTML. |

## Правила извлечения до получения реальной карточки

- Строго совпадающий SKU обязателен; данные соседнего товара не используются.
- Цена: первый корректный `offers.price`, затем `product.price`; конкретный коммерческий тип цены ещё не определён.
- `art_set`: артикул производителя → `art set` → комплектация → состав набора. Приоритет может потребовать уточнения после просмотра реальных характеристик.
- Фото и видео продавца читаются только из полей выбранного товара; фотографии отзывов не считаются фотографиями продавца.
- `has_rich_content` учитывает HTML в описании, `richContent` товара и отдельный виджет, привязанный к тому же SKU.

## Ограничения и бонус

Airflow и DataLens — необязательные бонусы после работающего основного сценария. Условия доступа к Ozon, допустимый объём запросов и тестовый аккаунт разумно согласовать с заказчиком. Cookies, OAuth-токены, коды подтверждения и диагностический HTML не должны попадать в Git. Диагностическая копия HTML проходит лишь частичное удаление чувствительных данных и требует ручной проверки перед передачей.

Порядок дальнейшей работы и критерии завершения: [`remaining-tasks.md`](remaining-tasks.md).
